description: Error

content:
  application/json:
    schema:
      $ref: "../models/error.yml"

    example:
      error:
        code: not_found
        message: card not found
        status_code: 404
