description: Error

content:
  application/json:
    schema:
      $ref: "../models/error.yml"

    example:
      error:
        code: invalid
        message: message about incorrect parameter
        status_code: 422
