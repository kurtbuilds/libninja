description: Error

content:
  application/json:
    schema:
      $ref: "../models/error.yml"

    example:
      error:
        code: not_found
        message: to address not found
        status_code: 404
