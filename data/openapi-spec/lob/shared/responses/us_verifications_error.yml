description: Error

content:
  application/json:
    schema:
      $ref: "../models/error.yml"

    example:
      error:
        code: invalid
        message: primary_line is required or address is required
        status_code: 422
