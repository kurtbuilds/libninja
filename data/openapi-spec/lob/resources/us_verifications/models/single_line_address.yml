type: object

required:
  - address

properties:
  address:
    type: string
    description: >
      The entire address in one string (e.g., "210 King Street 94107").
      _Does not support a recipient and will error when other payload parameters are provided._
    maxLength: 500
