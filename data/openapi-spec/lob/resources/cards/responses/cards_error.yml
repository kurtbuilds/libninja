description: Error

content:
  application/json:
    schema:
      $ref: "../../../shared/models/error.yml"

    example:
      error: # replace these properties with an error relevant to the specific resource
        code: not_found
        message: card not found
        status_code: 404
