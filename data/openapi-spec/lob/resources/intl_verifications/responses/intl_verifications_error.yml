description: Error

content:
  application/json:
    schema:
      $ref: "../../../shared/models/error.yml"

    example:
      error:
        code: invalid
        message: country is required
        status_code: 422
